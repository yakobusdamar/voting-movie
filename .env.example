# Base URL webhook n8n di sumopod.
# Ganti dengan URL instance n8n kamu.
VITE_API_BASE=https://<instance>.sumopod.com/webhook

# Token JWT (HS256) untuk autentikasi ke n8n.
# Simpan di .env.local, JANGAN commit.
VITE_API_TOKEN=<isi_token_jwt_anda>
